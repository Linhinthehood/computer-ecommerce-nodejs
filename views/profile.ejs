    <!DOCTYPE html>
    <html lang="en">

    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
        <meta name="description" content="" />
        <meta name="author" content="" />
        <title>Profile - We Sell Clothes</title>
        <!-- Font Awesome for icons -->
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
        <!-- Bootstrap icons-->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css" rel="stylesheet" />
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
            integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
        <!-- Core theme CSS (includes Bootstrap)-->
    </head>
    <style>
        body {
            background-color: #f8f9fa;
        }

        .card {
            border: none;
            box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075);
            transition: transform 0.3s ease;
        }

        .card:hover {
            transform: translateY(-5px);
        }

        .profile-header {
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            padding: 2rem 0;
            margin-bottom: 2rem;
        }

        .profile-avatar {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            object-fit: cover;
            border: 5px solid white;
            box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
        }

        .profile-stats {
            background: white;
            border-radius: 10px;
            padding: 1.5rem;
            margin-bottom: 1rem;
        }

        .stat-item {
            text-align: center;
        }

        .stat-value {
            font-size: 1.5rem;
            font-weight: bold;
            color: #212529;
        }

        .stat-label {
            color: #6c757d;
            font-size: 0.875rem;
        }

        .profile-section {
            background: white;
            border-radius: 10px;
            padding: 1.5rem;
            margin-bottom: 1.5rem;
        }

        .section-title {
            color: #212529;
            font-weight: 600;
            margin-bottom: 1.5rem;
            padding-bottom: 0.5rem;
            border-bottom: 2px solid #e9ecef;
        }

        .info-item {
            display: flex;
            align-items: center;
            margin-bottom: 1rem;
        }

        .info-icon {
            width: 40px;
            height: 40px;
            background: #f8f9fa;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-right: 1rem;
        }

        .info-content {
            flex: 1;
        }

        .info-label {
            color: #6c757d;
            font-size: 0.875rem;
            margin-bottom: 0.25rem;
        }

        .info-value {
            color: #212529;
            font-weight: 500;
        }

        .btn-edit {
            background: #212529;
            color: white;
            border: none;
            padding: 0.5rem 1.5rem;
            border-radius: 5px;
            transition: all 0.3s ease;
        }

        .btn-edit:hover {
            background: #343a40;
            transform: translateY(-2px);
        }

        .navbar {
            transition: all 0.3s ease-in-out;
        }

        .navbar.fixed-top {
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            background-color: rgba(255, 255, 255, 0.95);
        }

        .form-control:focus {
            border-color: #f04e30;
            box-shadow: 0 0 5px rgba(240, 78, 48, 0.5);
        }

        .btn-dark:hover {
            background-color: #f04e30;
            border-color: #f04e30;
        }

        .text-dark:hover {
            color: #f04e30 !important;
        }
    </style>

    <body>
        <!--cái này là phần Navigation-->
        <nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm fixed-top">
            <div class="container">
                <a class="navbar-brand fw-bold text-uppercase" href="/">We Sell Clothes</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                    aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse justify-content-between" id="navbarNav">
                    <ul class="navbar-nav">
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" id="navbarDropdown" href="#" role="button"
                                data-bs-toggle="dropdown" aria-expanded="false">Shop</a>
                            <ul class="dropdown-menu">
                                <li><a class="dropdown-item" href="/">All Products</a></li>
                                <li>
                                    <hr class="dropdown-divider" />
                                </li>
                                <li><a class="dropdown-item" href="/">New Arrivals</a></li>
                                <li><a class="dropdown-item" href="/shirts">Shirts</a></li>
                                <li><a class="dropdown-item" href="/bottoms">Bottoms</a></li>
                                <li><a class="dropdown-item" href="/outerwears">Outerwears</a></li>
                                <li><a class="dropdown-item" href="/accessories">Accessories</a></li>
                            </ul>
                        </li>
                        <li class="nav-item"><a class="nav-link" href="/aboutus">About</a></li>
                    </ul>

                    <!-- Phần search -->
                    <form class="d-flex me-3" action="/search" method="GET">
                        <input class="form-control form-control-sm border-dark" type="search" name="query"
                            placeholder="Search" aria-label="Search" required>
                        <button class="btn btn-dark btn-sm ms-2" type="submit">
                            <i class="bi bi-search"></i>
                        </button>
                    </form>

                    <!-- Phần giỏ hàng và đăng nhập -->
                    <div class="d-flex align-items-center gap-3">
                        <% if (user) { %>
                            <div class="dropdown">
                                <a href="#" class="text-dark fw-bold text-uppercase small dropdown-toggle"
                                    data-bs-toggle="dropdown" aria-expanded="false" style="text-decoration: none;">
                                    <%= user.name %> <i class="bi bi-person-fill fs-4"></i>
                                </a>
                                <ul class="dropdown-menu">
                                    <li><a class="dropdown-item" href="/profile">Profile</a></li>
                                    <li>
                                        <hr class="dropdown-divider">
                                    </li>
                                    <li><a class="dropdown-item" href="/logout">Logout</a></li>
                                </ul>
                            </div>
                            <% } else { %>
                                <a href="/auth" class="text-dark fw-bold text-uppercase small">
                                    Login / Register <i class="bi bi-person-fill fs-4"></i>
                                </a>
                                <% } %>
                                    <button id="cartButton" class="btn btn-dark btn-sm ms-2">
                                        <i class="bi bi-cart-fill me-1"></i>
                                        <span id="cartCount" class="badge bg-dark text-white ms-1 rounded-pill">0</span>
                                    </button>
                    </div>
                </div>
            </div>
        </nav>
        <!-- Modal giỏ hàng -->
        <div id="cartModal" class="modal fade" tabindex="-1">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title">Giỏ hàng</h5>
                        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                    </div>
                    <div class="modal-body">
                        <ul id="cartItems" class="list-group"></ul>
                    </div>
                    <div class="modal-footer">
                        <button id="clearCart" class="btn btn-danger me-auto">Xóa tất cả</button>
                        <a href="/cart" class="btn btn-primary">Go to Cart</a>
                    </div>
                </div>
            </div>
        </div>
        <!-- CSS cho cái navigation -->
        <style>
            .navbar {
                transition: all 0.3s ease-in-out;
            }

            .navbar.fixed-top {
                box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
                background-color: rgba(255, 255, 255, 0.95);
            }

            .form-control:focus {
                border-color: #f04e30;
                box-shadow: 0 0 5px rgba(240, 78, 48, 0.5);
            }

            .btn-dark:hover {
                background-color: #f04e30;
                border-color: #f04e30;
            }

            .text-dark:hover {
                color: #f04e30 !important;
            }
        </style>

        <div style="margin-top: 80px;"></div>

        <!-- Main Content -->
        <main class="container py-4">
            <!-- Error Alert -->
            <% if (typeof error !== 'undefined' && error) { %>
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                    <%= error %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            <% } %>

            <div class="row">

                <!-- Profile Information -->
                <div class="col-md-6">
                    <div class="profile-section">
                        <h3 class="section-title">Personal Information</h3>
                        <div class="info-item">
                            <div class="info-icon">
                                <i class="bi bi-person-fill"></i>
                            </div>
                            <div class="info-content">
                                <div class="info-label">Full Name</div>
                                <div class="info-value">
                                    <%= user.name %>
                                </div>
                            </div>
                        </div>
                        <div class="info-item">
                            <div class="info-icon">
                                <i class="bi bi-envelope-fill"></i>
                            </div>
                            <div class="info-content">
                                <div class="info-label">Email Address</div>
                                <div class="info-value">
                                    <%= user.email %>
                                </div>
                            </div>
                        </div>
                        <div class="info-item">
                            <div class="info-icon">
                                <i class="bi bi-telephone-fill"></i>
                            </div>
                            <div class="info-content">
                                <div class="info-label">Phone Number</div>
                                <div class="info-value">
                                    <%= user.phone %>
                                </div>
                            </div>
                        </div>
                        <button class="btn btn-edit" data-bs-toggle="modal" data-bs-target="#editProfileModal">
                            <i class="bi bi-pencil-square me-2"></i>Edit Profile
                        </button>
                    </div>
                </div>

                <!-- Edit Profile Modal -->
                <div class="modal fade" id="editProfileModal" tabindex="-1" aria-labelledby="editProfileModalLabel" aria-hidden="true">
                    <div class="modal-dialog">
                        <div class="modal-content">
                            <div class="modal-header">
                                <h5 class="modal-title" id="editProfileModalLabel">Edit Profile</h5>
                                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                            </div>
                            <form action="/profile/update" method="POST">
                                <div class="modal-body">
                                    <div class="mb-3">
                                        <label for="editName" class="form-label">Full Name</label>
                                        <input type="text" class="form-control" id="editName" name="name" value="<%= user.name %>" required>
                                    </div>
                                    <div class="mb-3">
                                        <label for="editEmail" class="form-label">Email Address</label>
                                        <input type="email" class="form-control" id="editEmail" name="email" value="<%= user.email %>" required>
                                    </div>
                                    <div class="mb-3">
                                        <label for="editPhone" class="form-label">Phone Number</label>
                                        <input type="tel" class="form-control" id="editPhone" name="phone" value="<%= user.phone %>" required>
                                    </div>
                                    <div class="mb-3">
                                        <label for="currentPassword" class="form-label">Current Password</label>
                                        <input type="password" class="form-control" id="currentPassword" name="currentPassword" required>
                                    </div>
                                    <div class="mb-3">
                                        <label for="newPassword" class="form-label">New Password (leave blank if no change)</label>
                                        <input type="password" class="form-control" id="newPassword" name="newPassword">
                                    </div>
                                    <div class="mb-3">
                                        <label for="confirmPassword" class="form-label">Confirm New Password</label>
                                        <input type="password" class="form-control" id="confirmPassword" name="confirmPassword">
                                    </div>
                                </div>
                                <div class="modal-footer">
                                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                                    <button type="submit" class="btn btn-primary">Save Changes</button>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>

                <!-- Recent Orders -->
                <div class="col-md-6">
                    <div class="profile-section">
                    <h3 class="section-title row">
                        <div class="col-md-6">Recent Orders</div>
                        <div class="col-md-6">
                        <div class="stat-item">
                            <div class="stat-value"><%= receipts.length %></div>
                            <div class="stat-label">Orders</div>
                        </div>
                        </div>
                    </h3>
                
                        <!-- Thẻ div giới hạn chiều cao, cho phép cuộn nội bộ -->
                        <div class="orders-list" style="max-height: 400px; overflow-y: auto;">
                            <% if (receipts && receipts.length > 0) { %>
                            <!-- Lặp qua mỗi đơn hàng -->
                            <% receipts.forEach(receipt => { %>
                                <div class="border p-3 mb-3">
                                <!-- Thông tin cơ bản (luôn hiển thị) -->
                                <div class="order-basic d-flex justify-content-between align-items-center">
                                    <div>
                                    <p class="mb-1"><strong>Ngày đặt:</strong> <%= receipt.createdAt.toLocaleString() %></p>
                                    <p class="mb-1"><strong>Địa chỉ:</strong> <%= receipt.address %></p>
                                    <p class="mb-1"><strong>Tổng tiền:</strong> <%= (receipt.totalPrice).toLocaleString('vi-VN') %> ₫</p>
                                    </div>
                                    <!-- Nút mở modal -->
                                    <button class="btn btn-link text-decoration-none" 
                                            data-bs-toggle="modal"
                                            data-bs-target="#orderModal<%= receipt._id %>">
                                        <i class="fa fa-eye"></i> View Details
                                    </button>
                                </div>

                                <!-- Modal cho đơn hàng -->
                                <div class="modal fade" id="orderModal<%= receipt._id %>" tabindex="-1" aria-labelledby="orderModalLabel<%= receipt._id %>" aria-hidden="true">
                                    <div class="modal-dialog modal-lg">
                                        <div class="modal-content">
                                            <div class="modal-header">
                                                <h5 class="modal-title" id="orderModalLabel<%= receipt._id %>">Order Details</h5>
                                                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                            </div>
                                            <div class="modal-body">
                                                <div class="order-details">
                                                    <div class="row mb-4">
                                                        <div class="col-md-6">
                                                            <h6 class="fw-bold">Shipping Information</h6>
                                                            <p class="mb-1"><strong>Phương thức vận chuyển:</strong> <%= receipt.shippingMethod %></p>
                                                            <p class="mb-1"><strong>Địa chỉ:</strong> <%= receipt.address %></p>
                                                            <p class="mb-1"><strong>Quận/Huyện:</strong> <%= receipt.district %></p>
                                                            <p class="mb-1"><strong>Thành phố:</strong> <%= receipt.city %></p>
                                                        </div>
                                                        <div class="col-md-6">
                                                            <h6 class="fw-bold">Order Information</h6>
                                                            <p class="mb-1"><strong>Order Date:</strong> <%= receipt.createdAt.toLocaleString() %></p>
                                                            <p class="mb-1"><strong>Payment Method:</strong> <%= receipt.paymentMethod %></p>
                                                            <p class="mb-1"><strong>Total Amount:</strong> <%= (receipt.totalPrice).toLocaleString('vi-VN') %> ₫</p>
                                                        </div>
                                                    </div>

                                                    <h6 class="fw-bold mb-3">Order Items</h6>
                                                    <div class="table-responsive">
                                                        <table class="table">
                                                            <thead>
                                                                <tr>
                                                                    <th>Product</th>
                                                                    <th>Size</th>
                                                                    <th>Quantity</th>
                                                                    <th class="text-end">Price</th>
                                                                </tr>
                                                            </thead>
                                                            <tbody>
                                                                <% receipt.cartItems.forEach(item => { %>
                                                                <tr>
                                                                    <td><strong><%= item.name %></strong></td>
                                                                    <td><%= item.size !== 'No Size' ? item.size : '-' %></td>
                                                                    <td><%= item.quantity %></td>
                                                                    <td class="text-end"><%= (item.price * item.quantity).toLocaleString('vi-VN') %> ₫</td>
                                                                </tr>
                                                                <% }) %>
                                                                <tr class="table-active">
                                                                    <td colspan="3" class="text-end"><strong>Total:</strong></td>
                                                                    <td class="text-end"><strong><%= (receipt.totalPrice).toLocaleString('vi-VN') %> ₫</strong></td>
                                                                </tr>
                                                            </tbody>
                                                        </table>
                                                    </div>
                                                </div>
                                            </div>
                                            <div class="modal-footer">
                                                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        <% }) %>
                        <% } else { %>
                        <div class="text-center text-muted py-4">
                            <i class="bi bi-bag-x display-4"></i>
                            <p class="mt-3">No orders yet</p>
                        </div>
                        <% } %>
                    </div>
                </div>

            </div>
        </main>

        <!-- FOOTER -->
        <footer class="bg-dark text-light py-5 mt-auto fixed-bottom">
            <div class="container">
                <div class="row">
                    <div class="col-12 col-md-4 mb-4 mb-md-0">
                        <h5 class="mb-3 fw-bold">We Sell Clothes</h5>
                        <p>Quality clothing for everyday wear. Our products are designed with comfort and style in mind.</p>
                        <div class="d-flex gap-3 mt-3">
                            <a href="#" class="text-light"><i class="fab fa-facebook-f"></i></a>
                            <a href="#" class="text-light"><i class="fab fa-twitter"></i></a>
                            <a href="#" class="text-light"><i class="fab fa-instagram"></i></a>
                            <a href="#" class="text-light"><i class="fab fa-pinterest"></i></a>
                        </div>
                    </div>
                    <div class="col-6 col-md-2 mb-3 mb-md-0">
                        <h6 class="fw-bold mb-3">SHOP</h6>
                        <ul class="list-unstyled">
                            <li class="mb-2"><a href="#" class="text-light text-decoration-none">Tops</a></li>
                            <li class="mb-2"><a href="#" class="text-light text-decoration-none">Bottoms</a></li>
                            <li class="mb-2"><a href="#" class="text-light text-decoration-none">Outerwears</a></li>
                            <li class="mb-2"><a href="#" class="text-light text-decoration-none">Accessories</a></li>
                            <li class="mb-2"><a href="#" class="text-light text-decoration-none">New Arrivals</a></li>
                        </ul>
                    </div>
                    <div class="col-6 col-md-2 mb-3 mb-md-0">
                        <h6 class="fw-bold mb-3">CUSTOMER CARE</h6>
                        <ul class="list-unstyled">
                            <li class="mb-2"><a href="#" class="text-light text-decoration-none">Contact Us</a></li>
                            <li class="mb-2"><a href="#" class="text-light text-decoration-none">FAQs</a></li>
                            <li class="mb-2"><a href="#" class="text-light text-decoration-none">Returns & Exchanges</a>
                            </li>
                            <li class="mb-2"><a href="#" class="text-light text-decoration-none">Shipping Info</a></li>
                            <li class="mb-2"><a href="#" class="text-light text-decoration-none">Size Guide</a></li>
                        </ul>
                    </div>
                    <div class="col-12 col-md-4">
                        <h6 class="fw-bold mb-3">NEWSLETTER</h6>
                        <p>Subscribe to receive updates, access to exclusive deals, and more.</p>
                        <div class="input-group mb-3">
                            <input type="email" class="form-control" placeholder="Your email address" aria-label="Email">
                            <button class="btn btn-primary" type="button">Subscribe</button>
                        </div>
                        <div class="mt-3">
                            <h6 class="fw-bold mb-2">WE ACCEPT</h6>
                            <div class="d-flex gap-2">
                                <i class="fab fa-cc-visa fa-2x"></i>
                                <i class="fab fa-cc-mastercard fa-2x"></i>
                                <i class="fab fa-cc-amex fa-2x"></i>
                                <i class="fab fa-cc-paypal fa-2x"></i>
                            </div>
                        </div>
                    </div>
                </div>
                <hr class="mt-4 border-secondary">
                <div class="row">
                    <div class="col-12 text-center">
                        <p class="mb-0">&copy; 2025 TDTU. All rights reserved.</p>
                    </div>
                </div>
            </div>
        </footer>

        <style>
            footer a:hover {
                color: #f04e30;
                transition: 0.3s;
            }
        </style>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
        <script src="/main.js"></script>
    </body>

    </html>